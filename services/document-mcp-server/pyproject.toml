[project]
name = "carecontext-document-mcp-server"
version = "0.1.0"
description = "Document tooling MCP server for CareContext AI."
requires-python = ">=3.12"
dependencies = [
  "carecontext-contracts",
  "mcp>=1.0.0",
  "pypdf>=5.0.0"
]

[tool.uv.sources]
carecontext-contracts = { path = "../../packages/carecontext-contracts", editable = true }
